# 第22章：常用内置模块

掌握Python标准库中的常用模块,让编程事半功倍。

## 什么是内置模块?

想象你要开发一个程序,需要处理日期、读写文件、生成随机数...如果这些功能都要自己从零写,那太费时间了!好消息是,Python自带了一个超级丰富的"工具箱"——**标准库**。

**内置模块就是Python自带的、不需要额外安装的模块**,直接`import`就能用!

### 为什么要学习内置模块?

```python
# 不使用模块 - 自己实现很复杂
def get_current_date():
    # 要考虑闰年、月份天数、时区...
    # 写几百行代码还可能有bug!
    pass

# 使用模块 - 一行搞定
from datetime import datetime
now = datetime.now()
print(now)  # 2024-01-15 14:30:45
```

**内置模块的优势**:
- **省时间** - 不用重复造轮子
- **稳定可靠** - 经过大量测试
- **功能丰富** - 覆盖各种常见需求
- **性能优化** - 很多用C语言实现,速度快
- **免费开源** - 不花一分钱

## datetime - 日期时间模块

处理日期和时间是编程中最常见的需求之一。

### 获取当前日期时间

```python
from datetime import datetime, date, time

# 获取当前日期和时间
now = datetime.now()
print(now)  # 2024-01-15 14:30:45.123456
print(type(now))  # <class 'datetime.datetime'>

# 只获取日期
today = date.today()
print(today)  # 2024-01-15
print(type(today))  # <class 'datetime.date'>

# 只获取时间
current_time = datetime.now().time()
print(current_time)  # 14:30:45.123456
print(type(current_time))  # <class 'datetime.time'>

# 获取具体信息
print(f"年份: {now.year}")      # 2024
print(f"月份: {now.month}")     # 1
print(f"日期: {now.day}")       # 15
print(f"小时: {now.hour}")      # 14
print(f"分钟: {now.minute}")    # 30
print(f"秒数: {now.second}")    # 45
print(f"星期: {now.weekday()}")  # 0=周一, 6=周日
```

### 创建日期时间对象

```python
from datetime import datetime, date, time

# 创建具体的日期时间
birthday = datetime(2000, 5, 20, 10, 30, 0)
print(birthday)  # 2000-05-20 10:30:00

# 只创建日期
my_date = date(2024, 1, 15)
print(my_date)  # 2024-01-15

# 只创建时间
my_time = time(14, 30, 45)
print(my_time)  # 14:30:45

# 组合日期和时间
combined = datetime.combine(my_date, my_time)
print(combined)  # 2024-01-15 14:30:45
```

### 日期格式化

```python
from datetime import datetime

now = datetime.now()

# 常用格式
print(now.strftime("%Y-%m-%d"))              # 2024-01-15
print(now.strftime("%Y年%m月%d日"))           # 2024年01月15日
print(now.strftime("%H:%M:%S"))              # 14:30:45
print(now.strftime("%Y-%m-%d %H:%M:%S"))     # 2024-01-15 14:30:45
print(now.strftime("%Y/%m/%d %I:%M:%S %p"))  # 2024/01/15 02:30:45 PM

# 星期
print(now.strftime("%A"))   # Monday
print(now.strftime("%a"))   # Mon
print(now.strftime("%w"))   # 1 (星期一=1)

# 月份
print(now.strftime("%B"))   # January
print(now.strftime("%b"))   # Jan

# 完整示例
print(now.strftime("今天是 %Y年%m月%d日 %A %H:%M"))
# 今天是 2024年01月15日 Monday 14:30
```

**格式化代码说明**:
```python
%Y  年份(4位)   2024
%y  年份(2位)   24
%m  月份        01-12
%d  日期        01-31
%H  小时(24时制) 00-23
%I  小时(12时制) 01-12
%M  分钟        00-59
%S  秒          00-59
%p  AM/PM
%A  星期全称    Monday
%a  星期简称    Mon
%B  月份全称    January
%b  月份简称    Jan
%w  星期数      0-6 (0=周日)
```

### 字符串转日期时间

```python
from datetime import datetime

# 解析字符串为日期时间
date_str = "2024-01-15 14:30:45"
dt = datetime.strptime(date_str, "%Y-%m-%d %H:%M:%S")
print(dt)        # 2024-01-15 14:30:45
print(type(dt))  # <class 'datetime.datetime'>

# 各种格式
examples = [
    ("2024/01/15", "%Y/%m/%d"),
    ("15-01-2024", "%d-%m-%Y"),
    ("Jan 15, 2024", "%b %d, %Y"),
    ("2024年1月15日", "%Y年%m月%d日"),
]

for date_str, format_str in examples:
    dt = datetime.strptime(date_str, format_str)
    print(f"{date_str} -> {dt}")
```

### 日期时间计算

```python
from datetime import datetime, timedelta

now = datetime.now()

# 使用timedelta进行加减
tomorrow = now + timedelta(days=1)
yesterday = now - timedelta(days=1)
next_week = now + timedelta(weeks=1)
next_month = now + timedelta(days=30)  # 近似
an_hour_ago = now - timedelta(hours=1)
ten_minutes_later = now + timedelta(minutes=10)

print(f"现在: {now.strftime('%Y-%m-%d %H:%M')}")
print(f"明天: {tomorrow.strftime('%Y-%m-%d %H:%M')}")
print(f"昨天: {yesterday.strftime('%Y-%m-%d %H:%M')}")
print(f"下周: {next_week.strftime('%Y-%m-%d %H:%M')}")

# 计算时间差
birthday = datetime(2000, 5, 20)
age_delta = datetime.now() - birthday

print(f"距离出生已经: {age_delta.days} 天")
print(f"大约: {age_delta.days // 365} 岁")
print(f"总秒数: {age_delta.total_seconds()}")

# 计算两个日期的差
start_date = datetime(2024, 1, 1)
end_date = datetime(2024, 12, 31)
diff = end_date - start_date
print(f"2024年有 {diff.days} 天")
```

### 实战例子：生日倒计时

```python
from datetime import datetime, timedelta

def birthday_countdown(birth_month, birth_day):
    """计算距离下次生日还有多少天"""
    today = datetime.now()
    current_year = today.year

    # 今年的生日
    birthday_this_year = datetime(current_year, birth_month, birth_day)

    # 如果今年生日已过,计算明年的
    if birthday_this_year < today:
        birthday_this_year = datetime(current_year + 1, birth_month, birth_day)

    # 计算天数
    days_left = (birthday_this_year - today).days

    # 是否是今天
    if days_left == 0:
        return "今天就是你的生日!🎉"
    else:
        return f"距离下次生日还有 {days_left} 天"

# 使用
print(birthday_countdown(5, 20))  # 假设生日是5月20日
print(birthday_countdown(1, 15))  # 假设生日是1月15日
```

## collections - 专业容器

`collections`模块提供了比list、dict更强大的容器类型。

### Counter - 计数器

```python
from collections import Counter

# 统计元素出现次数
words = ["apple", "banana", "apple", "cherry", "banana", "apple", "date"]
counter = Counter(words)

print(counter)  # Counter({'apple': 3, 'banana': 2, 'cherry': 1, 'date': 1})

# 最常见的元素
print(counter.most_common(2))  # [('apple', 3), ('banana', 2)]
print(counter.most_common(1))  # [('apple', 3)]

# 访问计数
print(counter["apple"])   # 3
print(counter["grape"])   # 0 (不存在返回0,不会报错)

# 统计字符串
text = "hello world"
char_counter = Counter(text)
print(char_counter)  # Counter({'l': 3, 'o': 2, 'h': 1, 'e': 1, ...})

# 更新计数
counter.update(["apple", "banana"])
print(counter)  # apple变成4, banana变成3

# 减少计数
counter.subtract(["apple"])
print(counter["apple"])  # 3

# 获取总数
print(sum(counter.values()))  # 总共的水果数
```

### 实战例子：投票统计系统

```python
from collections import Counter

class VotingSystem:
    """简单的投票系统"""

    def __init__(self, candidates):
        """初始化候选人"""
        self.candidates = candidates
        self.votes = Counter()

    def vote(self, candidate):
        """投票"""
        if candidate in self.candidates:
            self.votes[candidate] += 1
            print(f"已为 {candidate} 投票")
            return True
        else:
            print(f"候选人 {candidate} 不存在")
            return False

    def get_results(self):
        """获取投票结果"""
        print("\n=== 投票结果 ===")
        total = sum(self.votes.values())

        for candidate, count in self.votes.most_common():
            percentage = (count / total * 100) if total > 0 else 0
            print(f"{candidate}: {count} 票 ({percentage:.1f}%)")

        print(f"\n总投票数: {total}")

    def get_winner(self):
        """获取获胜者"""
        if not self.votes:
            return None
        winner = self.votes.most_common(1)[0]
        return winner[0]

# 使用
voting = VotingSystem(["张三", "李四", "王五"])

# 模拟投票
voting.vote("张三")
voting.vote("李四")
voting.vote("张三")
voting.vote("王五")
voting.vote("张三")
voting.vote("李四")

# 查看结果
voting.get_results()
print(f"\n获胜者: {voting.get_winner()}")
```

### defaultdict - 带默认值的字典

```python
from collections import defaultdict

# 普通字典的问题
normal_dict = {}
# normal_dict["fruits"].append("apple")  # KeyError!

# defaultdict自动创建默认值
fruit_dict = defaultdict(list)
fruit_dict["fruits"].append("apple")
fruit_dict["fruits"].append("banana")
fruit_dict["vegetables"].append("carrot")

print(dict(fruit_dict))
# {'fruits': ['apple', 'banana'], 'vegetables': ['carrot']}

# 默认值为0(用于计数)
word_count = defaultdict(int)
words = ["hello", "world", "hello", "python"]
for word in words:
    word_count[word] += 1

print(dict(word_count))  # {'hello': 2, 'world': 1, 'python': 1}

# 默认值为set
tags = defaultdict(set)
tags["python"].add("programming")
tags["python"].add("scripting")
tags["java"].add("programming")

print(dict(tags))
# {'python': {'programming', 'scripting'}, 'java': {'programming'}}
```

### namedtuple - 命名元组

```python
from collections import namedtuple

# 创建命名元组类
Point = namedtuple("Point", ["x", "y"])
Person = namedtuple("Person", ["name", "age", "city"])

# 创建实例
p1 = Point(10, 20)
p2 = Point(x=30, y=40)

# 访问元素
print(p1.x, p1.y)  # 10 20
print(p1[0], p1[1])  # 也可以用索引

# 创建人物
person = Person("张三", 25, "北京")
print(f"{person.name}, {person.age}岁, 来自{person.city}")

# 转换为字典
print(person._asdict())
# {'name': '张三', 'age': 25, 'city': '北京'}

# 替换字段
person2 = person._replace(age=26)
print(person2)  # Person(name='张三', age=26, city='北京')
```

### deque - 双端队列

```python
from collections import deque

# 创建双端队列
queue = deque([1, 2, 3])

# 右侧操作
queue.append(4)         # 右侧添加
print(queue)            # deque([1, 2, 3, 4])
print(queue.pop())      # 4 右侧弹出

# 左侧操作
queue.appendleft(0)     # 左侧添加
print(queue)            # deque([0, 1, 2, 3])
print(queue.popleft())  # 0 左侧弹出

# 旋转
queue.rotate(1)   # 向右旋转1位
print(queue)      # deque([3, 1, 2])

queue.rotate(-1)  # 向左旋转1位
print(queue)      # deque([1, 2, 3])

# 限制长度
limited_queue = deque(maxlen=3)
for i in range(5):
    limited_queue.append(i)
    print(limited_queue)
# deque([0], maxlen=3)
# deque([0, 1], maxlen=3)
# deque([0, 1, 2], maxlen=3)
# deque([1, 2, 3], maxlen=3)  # 0被挤出
# deque([2, 3, 4], maxlen=3)  # 1被挤出
```

## random - 随机数模块

### 生成随机数

```python
import random

# 随机整数
print(random.randint(1, 10))        # 1到10之间(包含1和10)
print(random.randrange(0, 10))      # 0到9之间
print(random.randrange(0, 10, 2))   # 0,2,4,6,8中随机一个

# 随机浮点数
print(random.random())              # 0.0到1.0之间
print(random.uniform(1.5, 5.5))     # 1.5到5.5之间

# 随机选择
colors = ["red", "blue", "green", "yellow"]
print(random.choice(colors))        # 随机选一个

# 随机抽样(不重复)
print(random.sample(colors, 2))     # 随机选2个,不重复
print(random.sample(range(1, 50), 6))  # 模拟彩票

# 随机抽样(可重复)
print(random.choices(colors, k=5))  # 随机选5个,可能重复

# 带权重的随机选择
fruits = ["apple", "banana", "cherry"]
weights = [5, 3, 2]  # apple被选中概率最高
print(random.choices(fruits, weights=weights, k=10))

# 打乱列表
numbers = [1, 2, 3, 4, 5]
random.shuffle(numbers)
print(numbers)  # [3, 1, 5, 2, 4] (顺序随机)
```

### 设置随机种子

```python
import random

# 设置种子,使随机数可重现
random.seed(42)
print(random.randint(1, 100))  # 82

random.seed(42)  # 相同种子
print(random.randint(1, 100))  # 82 (相同结果)

# 用于调试和测试
def test_random_function():
    random.seed(123)  # 固定种子
    result = [random.randint(1, 10) for _ in range(5)]
    print(result)  # 每次运行结果相同

test_random_function()
```

### 实战例子：抽奖系统

```python
import random

class LotterySystem:
    """抽奖系统"""

    def __init__(self, prizes):
        """
        初始化奖品
        prizes: {奖品名: (数量, 概率权重)}
        """
        self.prizes = prizes
        self.history = []

    def draw(self):
        """抽奖"""
        # 分离奖品名和权重
        prize_names = []
        weights = []

        for name, (count, weight) in self.prizes.items():
            if count > 0:  # 还有库存
                prize_names.append(name)
                weights.append(weight)

        if not prize_names:
            return "奖品已抽完"

        # 按权重随机选择
        prize = random.choices(prize_names, weights=weights)[0]

        # 减少库存
        count, weight = self.prizes[prize]
        self.prizes[prize] = (count - 1, weight)

        # 记录历史
        self.history.append(prize)

        return prize

    def get_statistics(self):
        """获取抽奖统计"""
        from collections import Counter
        counter = Counter(self.history)

        print("\n=== 抽奖统计 ===")
        for prize, count in counter.most_common():
            print(f"{prize}: {count} 次")
        print(f"总抽奖次数: {len(self.history)}")

# 使用
lottery = LotterySystem({
    "一等奖": (1, 1),      # 1个,权重1
    "二等奖": (3, 5),      # 3个,权重5
    "三等奖": (10, 20),    # 10个,权重20
    "谢谢参与": (100, 74)  # 100个,权重74
})

# 模拟100次抽奖
print("开始抽奖:")
for i in range(100):
    prize = lottery.draw()
    if "等奖" in prize:  # 只显示中奖的
        print(f"第{i+1}次: 恭喜获得{prize}!")

lottery.get_statistics()
```

## itertools - 迭代工具

### 无限迭代器

```python
import itertools

# count - 无限计数
counter = itertools.count(start=10, step=2)
print(next(counter))  # 10
print(next(counter))  # 12
print(next(counter))  # 14

# 常用于配合zip创建编号
names = ["张三", "李四", "王五"]
for i, name in zip(itertools.count(1), names):
    print(f"{i}. {name}")
# 1. 张三
# 2. 李四
# 3. 王五

# cycle - 无限循环
colors = itertools.cycle(["red", "green", "blue"])
for i in range(7):
    print(next(colors))
# red, green, blue, red, green, blue, red

# repeat - 重复元素
for num in itertools.repeat(5, 3):
    print(num)  # 5, 5, 5
```

### 组合迭代器

```python
import itertools

# 排列(考虑顺序)
items = [1, 2, 3]
print(list(itertools.permutations(items, 2)))
# [(1, 2), (1, 3), (2, 1), (2, 3), (3, 1), (3, 2)]

# 组合(不考虑顺序)
print(list(itertools.combinations(items, 2)))
# [(1, 2), (1, 3), (2, 3)]

# 可重复的组合
print(list(itertools.combinations_with_replacement(items, 2)))
# [(1, 1), (1, 2), (1, 3), (2, 2), (2, 3), (3, 3)]

# 笛卡尔积
colors = ["red", "blue"]
sizes = ["S", "M", "L"]
print(list(itertools.product(colors, sizes)))
# [('red', 'S'), ('red', 'M'), ('red', 'L'),
#  ('blue', 'S'), ('blue', 'M'), ('blue', 'L')]
```

### 其他实用工具

```python
import itertools

# chain - 连接多个迭代器
list1 = [1, 2, 3]
list2 = [4, 5, 6]
list3 = [7, 8, 9]
chained = itertools.chain(list1, list2, list3)
print(list(chained))  # [1, 2, 3, 4, 5, 6, 7, 8, 9]

# compress - 根据选择器过滤
data = ["A", "B", "C", "D", "E"]
selectors = [1, 0, 1, 0, 1]  # 1表示保留
print(list(itertools.compress(data, selectors)))  # ['A', 'C', 'E']

# dropwhile - 删除元素直到条件为False
numbers = [1, 3, 5, 6, 7, 8, 9]
result = itertools.dropwhile(lambda x: x < 6, numbers)
print(list(result))  # [6, 7, 8, 9]

# takewhile - 获取元素直到条件为False
numbers = [1, 3, 5, 6, 7, 8, 9]
result = itertools.takewhile(lambda x: x < 6, numbers)
print(list(result))  # [1, 3, 5]

# groupby - 分组
data = [("A", 1), ("A", 2), ("B", 3), ("B", 4), ("C", 5)]
for key, group in itertools.groupby(data, lambda x: x[0]):
    print(f"{key}: {list(group)}")
# A: [('A', 1), ('A', 2)]
# B: [('B', 3), ('B', 4)]
# C: [('C', 5)]
```

## functools - 函数工具

### lru_cache - 缓存装饰器

```python
from functools import lru_cache
import time

# 斐波那契数列(低效版本)
def fib_slow(n):
    if n < 2:
        return n
    return fib_slow(n-1) + fib_slow(n-2)

# 使用缓存优化
@lru_cache(maxsize=None)
def fib_fast(n):
    if n < 2:
        return n
    return fib_fast(n-1) + fib_fast(n-2)

# 对比性能
start = time.time()
print(fib_slow(30))
print(f"慢速版本耗时: {time.time() - start:.2f}秒")

start = time.time()
print(fib_fast(30))
print(f"快速版本耗时: {time.time() - start:.4f}秒")

# 查看缓存信息
print(fib_fast.cache_info())
```

### partial - 偏函数

```python
from functools import partial

# 原函数
def power(base, exponent):
    return base ** exponent

# 创建偏函数
square = partial(power, exponent=2)
cube = partial(power, exponent=3)

print(square(5))  # 25
print(cube(5))    # 125

# 实际应用:固定某些参数
def log(message, level="INFO"):
    print(f"[{level}] {message}")

# 创建专用日志函数
error_log = partial(log, level="ERROR")
warning_log = partial(log, level="WARNING")

log("正常信息")          # [INFO] 正常信息
error_log("出错了")      # [ERROR] 出错了
warning_log("警告信息")  # [WARNING] 警告信息
```

### reduce - 累积计算

```python
from functools import reduce

# 计算乘积
numbers = [1, 2, 3, 4, 5]
product = reduce(lambda x, y: x * y, numbers)
print(product)  # 120

# 计算最大值
numbers = [34, 12, 78, 23, 45]
max_num = reduce(lambda x, y: x if x > y else y, numbers)
print(max_num)  # 78

# 连接字符串
words = ["Hello", "World", "Python"]
sentence = reduce(lambda x, y: x + " " + y, words)
print(sentence)  # Hello World Python
```

## pathlib - 现代路径操作

```python
from pathlib import Path

# 获取当前目录
current_dir = Path.cwd()
print(current_dir)

# 用户主目录
home = Path.home()
print(home)

# 路径拼接(使用 / 运算符,很优雅!)
file_path = Path("data") / "files" / "example.txt"
print(file_path)  # data/files/example.txt

# 检查存在
if file_path.exists():
    print("文件存在")

# 检查类型
path = Path(".")
print(path.is_file())  # False
print(path.is_dir())   # True

# 创建目录
new_dir = Path("data/new_folder")
new_dir.mkdir(parents=True, exist_ok=True)

# 读写文件
file = Path("test.txt")
file.write_text("Hello, World!", encoding="utf-8")
content = file.read_text(encoding="utf-8")
print(content)

# 遍历目录
for item in Path(".").iterdir():
    if item.is_file():
        print(f"文件: {item.name}")
    elif item.is_dir():
        print(f"目录: {item.name}")

# glob模式匹配
for py_file in Path(".").glob("*.py"):
    print(py_file)

# 递归匹配
for py_file in Path(".").rglob("*.py"):
    print(py_file)

# 路径信息
path = Path("data/files/example.txt")
print(path.name)        # example.txt
print(path.stem)        # example
print(path.suffix)      # .txt
print(path.parent)      # data/files
print(path.parts)       # ('data', 'files', 'example.txt')
```

## os和sys - 系统相关

### os - 操作系统接口

```python
import os

# 当前工作目录
print(os.getcwd())

# 改变目录
# os.chdir("/path/to/directory")

# 列出目录内容
files = os.listdir(".")
print(files)

# 创建目录
os.makedirs("test/sub/dir", exist_ok=True)

# 删除目录
# os.rmdir("test/sub/dir")  # 只能删除空目录
# os.removedirs("test/sub/dir")  # 递归删除空目录

# 删除文件
# os.remove("file.txt")

# 重命名
# os.rename("old.txt", "new.txt")

# 执行系统命令
# os.system("ls -l")  # Linux/Mac
# os.system("dir")    # Windows

# 环境变量
print(os.environ.get("PATH"))
print(os.environ.get("HOME"))

# 路径操作
path = os.path.join("folder", "subfolder", "file.txt")
print(path)

print(os.path.exists("test.txt"))
print(os.path.isfile("test.txt"))
print(os.path.isdir("folder"))

# 获取文件信息
if os.path.exists("test.txt"):
    print(os.path.getsize("test.txt"))  # 字节数
    print(os.path.getmtime("test.txt")) # 修改时间戳
    print(os.path.getctime("test.txt")) # 创建时间戳

# 分割路径
full_path = "/home/user/documents/file.txt"
dirname = os.path.dirname(full_path)   # /home/user/documents
basename = os.path.basename(full_path) # file.txt
print(dirname, basename)
```

### sys - Python解释器相关

```python
import sys

# Python版本信息
print(sys.version)        # 详细版本
print(sys.version_info)   # 版本元组
print(f"Python {sys.version_info.major}.{sys.version_info.minor}")

# 平台信息
print(sys.platform)  # linux, darwin, win32

# 命令行参数
print(sys.argv)  # ['script.py', 'arg1', 'arg2']

# 模块搜索路径
for path in sys.path:
    print(path)

# 标准输入输出
sys.stdout.write("Hello\n")
# line = sys.stdin.readline()

# 获取对象大小
import sys
numbers = [1, 2, 3, 4, 5]
print(f"列表大小: {sys.getsizeof(numbers)} 字节")

# 退出程序
# sys.exit(0)  # 正常退出
# sys.exit(1)  # 异常退出
```

## 实战例子

### 例子1：文件整理工具

```python
from pathlib import Path
import shutil
from datetime import datetime

class FileOrganizer:
    """按文件类型整理文件"""

    def __init__(self, source_dir):
        self.source_dir = Path(source_dir)
        self.file_types = {
            "图片": [".jpg", ".jpeg", ".png", ".gif", ".bmp"],
            "文档": [".txt", ".doc", ".docx", ".pdf", ".xlsx"],
            "视频": [".mp4", ".avi", ".mkv", ".mov"],
            "音频": [".mp3", ".wav", ".flac"],
            "压缩包": [".zip", ".rar", ".7z", ".tar", ".gz"]
        }

    def organize(self):
        """整理文件"""
        if not self.source_dir.exists():
            print(f"目录不存在: {self.source_dir}")
            return

        # 创建分类文件夹
        for category in self.file_types.keys():
            category_dir = self.source_dir / category
            category_dir.mkdir(exist_ok=True)

        # 移动文件
        moved_count = 0
        for file in self.source_dir.iterdir():
            if file.is_file():
                # 查找文件类型
                category = self._get_category(file.suffix.lower())
                if category:
                    dest = self.source_dir / category / file.name

                    # 处理重名
                    if dest.exists():
                        timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
                        dest = dest.parent / f"{dest.stem}_{timestamp}{dest.suffix}"

                    shutil.move(str(file), str(dest))
                    print(f"移动: {file.name} -> {category}/")
                    moved_count += 1

        print(f"\n整理完成! 共移动 {moved_count} 个文件")

    def _get_category(self, suffix):
        """根据后缀获取分类"""
        for category, extensions in self.file_types.items():
            if suffix in extensions:
                return category
        return None

# 使用
# organizer = FileOrganizer("Downloads")
# organizer.organize()
```

### 例子2：日程提醒系统

```python
from datetime import datetime, timedelta
from collections import defaultdict

class ScheduleReminder:
    """日程提醒系统"""

    def __init__(self):
        self.schedules = defaultdict(list)

    def add_schedule(self, date_str, time_str, event):
        """添加日程"""
        try:
            dt = datetime.strptime(f"{date_str} {time_str}", "%Y-%m-%d %H:%M")
            self.schedules[date_str].append({
                "time": time_str,
                "event": event,
                "datetime": dt
            })
            print(f"已添加日程: {date_str} {time_str} {event}")
            return True
        except ValueError as e:
            print(f"日期格式错误: {e}")
            return False

    def get_today_schedule(self):
        """获取今天的日程"""
        today = datetime.now().strftime("%Y-%m-%d")
        schedules = self.schedules.get(today, [])

        if not schedules:
            print("今天没有日程安排")
            return

        print(f"\n=== {today} 的日程 ===")
        for schedule in sorted(schedules, key=lambda x: x["time"]):
            print(f"{schedule['time']} - {schedule['event']}")

    def get_upcoming_events(self, days=7):
        """获取未来几天的日程"""
        now = datetime.now()
        upcoming = []

        for date_str, schedules in self.schedules.items():
            for schedule in schedules:
                dt = schedule["datetime"]
                if now < dt <= now + timedelta(days=days):
                    days_left = (dt - now).days
                    upcoming.append({
                        "date": date_str,
                        "time": schedule["time"],
                        "event": schedule["event"],
                        "days_left": days_left
                    })

        if not upcoming:
            print(f"未来{days}天没有日程安排")
            return

        print(f"\n=== 未来{days}天的日程 ===")
        for item in sorted(upcoming, key=lambda x: (x["date"], x["time"])):
            if item["days_left"] == 0:
                time_label = "今天"
            elif item["days_left"] == 1:
                time_label = "明天"
            else:
                time_label = f"{item['days_left']}天后"

            print(f"{item['date']} {item['time']} - {item['event']} ({time_label})")

# 使用
reminder = ScheduleReminder()
reminder.add_schedule("2024-01-15", "09:00", "团队会议")
reminder.add_schedule("2024-01-15", "14:30", "项目评审")
reminder.add_schedule("2024-01-16", "10:00", "客户拜访")
reminder.add_schedule("2024-01-20", "15:00", "培训课程")

reminder.get_today_schedule()
reminder.get_upcoming_events(7)
```

### 例子3：密码生成器

```python
import random
import string

class PasswordGenerator:
    """安全密码生成器"""

    def __init__(self):
        self.lowercase = string.ascii_lowercase
        self.uppercase = string.ascii_uppercase
        self.digits = string.digits
        self.special = "!@#$%^&*()_+-=[]{}|;:,.<>?"

    def generate(self, length=12, use_uppercase=True, use_digits=True,
                 use_special=True, exclude_ambiguous=True):
        """
        生成密码

        参数:
            length: 密码长度
            use_uppercase: 是否包含大写字母
            use_digits: 是否包含数字
            use_special: 是否包含特殊字符
            exclude_ambiguous: 是否排除易混淆字符(如 0,O,l,1)
        """
        # 构建字符池
        chars = self.lowercase

        if use_uppercase:
            chars += self.uppercase
        if use_digits:
            chars += self.digits
        if use_special:
            chars += self.special

        # 排除易混淆字符
        if exclude_ambiguous:
            ambiguous = "0Ol1"
            chars = "".join(c for c in chars if c not in ambiguous)

        # 确保至少包含各种字符类型
        password = []

        if use_uppercase:
            password.append(random.choice(self.uppercase))
        if use_digits:
            password.append(random.choice(self.digits))
        if use_special:
            password.append(random.choice(self.special))

        password.append(random.choice(self.lowercase))

        # 填充剩余长度
        remaining = length - len(password)
        password.extend(random.choices(chars, k=remaining))

        # 打乱顺序
        random.shuffle(password)

        return "".join(password)

    def generate_multiple(self, count=5, **kwargs):
        """生成多个密码"""
        passwords = []
        for i in range(count):
            pwd = self.generate(**kwargs)
            strength = self._check_strength(pwd)
            passwords.append((pwd, strength))
        return passwords

    def _check_strength(self, password):
        """检查密码强度"""
        score = 0

        if len(password) >= 8:
            score += 1
        if len(password) >= 12:
            score += 1
        if any(c.islower() for c in password):
            score += 1
        if any(c.isupper() for c in password):
            score += 1
        if any(c.isdigit() for c in password):
            score += 1
        if any(c in self.special for c in password):
            score += 1

        if score >= 5:
            return "强"
        elif score >= 3:
            return "中"
        else:
            return "弱"

# 使用
generator = PasswordGenerator()

print("=== 生成5个强密码 ===")
passwords = generator.generate_multiple(
    count=5,
    length=16,
    use_uppercase=True,
    use_digits=True,
    use_special=True
)

for pwd, strength in passwords:
    print(f"{pwd} (强度: {strength})")
```

## 常见陷阱

### 陷阱1：datetime的坑

```python
from datetime import datetime

# ❌ 错误 - 直接相减字符串
# date1 = "2024-01-15"
# date2 = "2024-01-10"
# diff = date1 - date2  # TypeError

# ✅ 正确 - 转换为datetime对象
date1 = datetime.strptime("2024-01-15", "%Y-%m-%d")
date2 = datetime.strptime("2024-01-10", "%Y-%m-%d")
diff = date1 - date2
print(diff.days)  # 5

# ❌ 错误 - 时区问题
now = datetime.now()  # 本地时间,没有时区信息
# 与其他时区比较会出问题

# ✅ 正确 - 使用时区
from datetime import timezone
now_utc = datetime.now(timezone.utc)  # UTC时间
```

### 陷阱2：random的种子

```python
import random

# ❌ 问题 - 多次运行结果不同
def get_random_numbers():
    return [random.randint(1, 100) for _ in range(5)]

# 第一次: [34, 78, 12, 56, 90]
# 第二次: [23, 45, 67, 11, 88] (不同)

# ✅ 使用种子保证可重复
def get_random_numbers_reproducible():
    random.seed(42)  # 固定种子
    return [random.randint(1, 100) for _ in range(5)]

# 每次运行结果相同,用于测试和调试
```

### 陷阱3：路径分隔符

```python
import os

# ❌ 错误 - 硬编码路径分隔符
path = "data\\files\\example.txt"  # 只在Windows上工作

# ✅ 正确 - 使用os.path.join
path = os.path.join("data", "files", "example.txt")
# Windows: data\files\example.txt
# Linux/Mac: data/files/example.txt

# ✅ 更好 - 使用pathlib
from pathlib import Path
path = Path("data") / "files" / "example.txt"
```

### 陷阱4：Counter的陷阱

```python
from collections import Counter

# ❌ 错误 - 访问不存在的key不会报错,返回0
counter = Counter(["a", "b", "c"])
print(counter["d"])  # 0 (不存在,但不报错)

# 可能让你以为"d"出现了0次,但实际上根本不存在
# 小心判断

# ✅ 正确 - 检查key是否存在
if "d" in counter:
    print(counter["d"])
else:
    print("d不存在")
```

## 最佳实践

### 1. 使用pathlib代替os.path

```python
# ❌ 旧方法
import os
path = os.path.join("data", "file.txt")
if os.path.exists(path):
    with open(path) as f:
        content = f.read()

# ✅ 新方法
from pathlib import Path
path = Path("data") / "file.txt"
if path.exists():
    content = path.read_text()
```

### 2. 使用缓存优化递归

```python
# ❌ 低效
def fibonacci(n):
    if n < 2:
        return n
    return fibonacci(n-1) + fibonacci(n-2)

# ✅ 高效
from functools import lru_cache

@lru_cache(maxsize=None)
def fibonacci(n):
    if n < 2:
        return n
    return fibonacci(n-1) + fibonacci(n-2)
```

### 3. 使用Counter统计

```python
# ❌ 手动统计
word_count = {}
for word in words:
    if word in word_count:
        word_count[word] += 1
    else:
        word_count[word] = 1

# ✅ 使用Counter
from collections import Counter
word_count = Counter(words)
```

### 4. 使用defaultdict避免KeyError

```python
# ❌ 需要检查key
groups = {}
for item in items:
    key = item.category
    if key not in groups:
        groups[key] = []
    groups[key].append(item)

# ✅ 使用defaultdict
from collections import defaultdict
groups = defaultdict(list)
for item in items:
    groups[item.category].append(item)
```

## 练习题

### 练习1：时间计算器

创建一个程序,计算:
- 两个日期之间相差多少天
- 距离下一个生日还有多少天
- 某个日期是星期几
- 计算年龄(精确到天)

### 练习2：文件统计工具

编写程序统计目录中:
- 各类文件的数量
- 各类文件的总大小
- 最大和最小的文件
- 最近修改的10个文件

### 练习3：抽奖模拟器

实现一个抽奖系统:
- 设置不同奖品和概率
- 模拟N次抽奖
- 统计各奖品中奖次数
- 验证概率是否符合预期

### 练习4：日志分析器

读取日志文件,统计:
- 每小时的访问量
- 最常访问的10个页面
- 错误率
- 平均响应时间

### 练习5：任务调度器

创建一个简单的任务调度器:
- 添加定时任务
- 检查到期任务
- 执行任务
- 记录执行历史

## 下一步

学会了常用内置模块,下一章我们学习正则表达式,处理复杂的文本匹配!

[上一章:第21章 - 面向对象进阶 ←](../21-面向对象进阶/21-面向对象进阶.md)

[下一章:第23章 - 正则表达式 →](../23-正则表达式/23-正则表达式.md)

---

**本章重点**
- ✅ 掌握datetime处理日期时间
- ✅ 使用collections的专业容器
- ✅ 掌握random生成随机数
- ✅ 使用itertools进行迭代操作
- ✅ 掌握functools函数工具
- ✅ 使用pathlib进行路径操作
- ✅ 了解os和sys系统接口
- ✅ 避免常见陷阱

**记住**
- 优先使用pathlib代替os.path
- 使用lru_cache优化递归
- Counter自动初始化为0
- defaultdict避免KeyError
- datetime需要注意时区
- 随机数用seed保证可重复
